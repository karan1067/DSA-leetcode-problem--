#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int size,element;
    scanf("%d",&size);
    
    int arr[size],i,r=0;
    for(i=0 ; i<size ; i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&element);
    
    for(i=0 ; i<size ; i++){
        if(arr[i] == element){
            r = 1;
            break;
        }
    }
    if(r == 1){
        printf("Found");
    }else{
        printf("Not Found");
    }

    return 0;
}
